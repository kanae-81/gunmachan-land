export interface FerrisWheelOptions {
  duration: number;
  displaySize: string;
  marginRatio: number;
  root?: HTMLElement;
  imgArray?: string[];
}

export interface MerryGoRoundOptions {
  duration: number;
  displaySize: string;
  marginRatio: number;
  root?: HTMLElement;
  imgArray?: string[];
}

export interface SkyDivingOptions {
  displaySize: string;
  speed: number;
  root?: HTMLElement;
  imgArray?: string[];
}

export interface CoffeeCupOptions {
  displayCount: number;
  displaySize: string;
  defaultSpeed: number;
  fastRatio: number;
  root?: HTMLElement;
  imgArray?: string[];
}

export interface AccompanyOptions {
  displayCount: number;
  displaySize: string;
  interval: number;
  root?: HTMLElement;
  imgArray?: string[];
}
